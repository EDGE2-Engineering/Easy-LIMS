# Caddy reverse proxy configuration
# Serves HTTP on port 80 and auto-provisions Let's Encrypt SSL when a domain is specified

:80 {
    reverse_proxy fastapi:8000
}

# To enable automatic SSL with your custom domain, uncomment and replace with your domain:
# yourdomain.com {
#     reverse_proxy fastapi:8000
# }
